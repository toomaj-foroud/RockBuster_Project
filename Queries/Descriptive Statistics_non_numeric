# Descriptive statistics of non_numeric variables

SELECT MODE() WITHIN GROUP (ORDER BY release_year)
AS modal_release_year,
 MODE() WITHIN GROUP (ORDER BY language_id)
AS modal_language_id,
 MODE() WITHIN GROUP (ORDER BY rating)
AS modal_rating
FROM film
